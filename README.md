# Insignia-Subnet
The Insignia subnet utilizes the bittensor network to structure a decentralized quantitative trading firm. Researcher miners compete to produce predictive machine learning models, scored by validators against a proprietary tick-by-tick benchmark dataset, while trading-operations miners build execution strategies that inference those models. Rather than ranking the two skill sets separately, Insignia uses a **single incentive mechanism** that matches a researcher and a trader into a `(model, strategy)` pair and evaluates them together — like individuals in a genetic algorithm. Validators query one researcher miner and one trader miner per pair, score the pair on the combined model-quality and live-trading metrics, and select/breed pairs across generations with an NSGA-II-style multi-objective genetic algorithm. This produces battle-tested model+strategy pairs eligible for deployment by the subnet owner's prop trading firm. The mechanism is hardened against miner collusion, miner-validator collusion, and latency arbitrage via chain-seeded pairing, variance-penalized marginal-contribution credit, and commit-reveal. See [subnet/docs/PAIRING_MECHANISM.md](subnet/docs/PAIRING_MECHANISM.md).
## Economic model — fund-backed alpha flywheel

The subnet's alpha token is the **investment wrapper for the prop-trading fund**, not a growth asset: returns originate in the trading desk (which deploys the subnet's evolved `(model, strategy)` pairs), and holding staked alpha gives fund exposure. The treasury runs the structure as a **closed-end fund at a disciplined NAV band** it buys alpha with trading revenue only when spot trades below 0.9× depth-adjusted NAV per alpha (TWAP, limit-bounded, MEV-shielded), holds inside the band, and stops buying above it. LPs convert fund units into alpha delivered OTC and locked to the subnet-owner hotkey (instant conviction, subnet-ownership defense), with a 12-month perpetual lock followed by a staged exponential redemption that doubles as slippage control. Miner alignment matters because miner selling is NAV leakage: retention incentives (fund exposure) keep issuance inside the wrapper. The full mechanism design, protocol corrections, and milestones are in [docs/SPEC.md](docs/SPEC.md); risks in [docs/RISK_REGISTER.md](docs/RISK_REGISTER.md); quantitative model charts in [docs/investor/](docs/investor/).

**Repository layout:** [subnet/](subnet/) is the Bittensor subnet (miners, validators, pairing mechanism). The fund layer lives at the root: [treasury/](treasury/) (NAV-band engine, pool math, execution), [lockmgr/](lockmgr/) (LP lock lifecycle), [otc/](otc/) (quotes and settlement, gated on Phase-0 legal signoff), [risk/](risk/) (reflexivity Monte Carlo, alerts), [dashboards/](dashboards/) (investor charts and factsheet), [chainio/](chainio/) (live chain-parameter reads). Fund-layer tests: `python -m pytest tests/`.

The subnet utilizes decentralization and open source to diversify strategies using predictive modeling for high frequency trading, and incentivise competition to stay current with market state transitions.
