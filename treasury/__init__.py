"""Treasury engine: revenue → TAO → alpha pipeline under the NAV-band policy
(SPEC §0.5, §5). `pool_math` and `emissions` are pure; `policy` decides;
`accounting` values; `engine` orchestrates; `execution` touches the chain.
"""
